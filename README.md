# Moon & Pearl Lutherie

A boutique GitHub Pages shrine / archive site for Moon & Pearl Lutherie.

## Local preview

Open `index.html` directly in a browser, or serve the folder with any static server.

Example:

```bash
python3 -m http.server 8000
```

Then visit:

```text
http://localhost:8000
```

## Deploying to GitHub Pages

1. Push the repo to GitHub.
2. In the repository settings, enable GitHub Pages.
3. Publish from the `main` branch and the repository root.
4. Add a `CNAME` file later if you want to point a custom domain at the site.

## Notes

- The site is intentionally static and lightweight.
- Replace the simulated gallery panels with real instrument photography when ready.
- The current copy is tuned for a quiet, boutique atelier tone.
